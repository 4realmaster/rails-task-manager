
<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li> <%= link_to task.title, task_path(task.id) %> <%= ' -- done' if task.completed %></li>
  <% end %>
</ul>


<%= link_to "Add a new task", new_path %>
